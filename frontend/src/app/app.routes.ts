import { Routes } from '@angular/router';
import { LoginComponent } from './component/home_page/login/login.component';


export const routes: Routes = [
    {
        path:'login',component:LoginComponent
    }
 
];
